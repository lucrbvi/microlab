### Microlab

This is a personal notebook to learn and experiment theories in the deep-learning space from math to code. Everything is designed to be simple enough to be easily understandable with basic knowledge in linear algebra and Python.

Also all the code is written in Python using [tinygrad](https://tinygrad.org/).

### Prerequisites

* Good understanding of [linear algebra](https://en.wikipedia.org/wiki/Linear_algebra) (matrices, derivative, vectors)
* Understand basics of [Python](https://www.python.org/) and programming

### Get started

I recommend you to [clone the repository](https://git-scm.com/docs/git-clone) and read the markdown files in Obsidian, VS Code markdown reader or other software you prefer.

Then you should install the packages by running `uv`

You can check in the `md/` directory to find the documents linked with scripts.

### Run scripts
```sh
python3 -m scripts.attention
```

or

```sh
uv run -m scripts.attention
```