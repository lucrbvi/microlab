[project]
name = "microlab"
version = "0.1.0"
description = "A notebook to learn and experiment deep-learning from math to code "
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy==2.4.2",
    "tinygrad==0.12.0",
]
